# Frontend (React/Vite) — diese Werte landen im Browser-Bundle.
# Der anon key ist bewusst öffentlich, siehe README "Warum darf der anon key öffentlich sein?".
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Nach dem Ausfüllen: Datei als .env speichern (nicht .env.example).
# .env wird nie committet (siehe .gitignore).
